name: Obsolete Pine warmup patch disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete workflow intentionally disabled; exact warmup is already in the Pine source."
